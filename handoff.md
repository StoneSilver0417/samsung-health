# RunLog - Handoff

## 현재 상태

- **버전**: v1.6.6+18 — 캘린더 일요일 시작 + 수동 기록 추가·편집·페이스 입력 모드 구현 완료 (2026-07-24)
- **빌드 상태**: 캘린더 일요일 시작 변경까지 로컬에서 `flutter analyze` 이슈 0건 확인. 아직 APK 빌드/설치 전
- **배포 상태**: GitHub 저장소 공개 전환 완료, v1.6.0~v1.6.3 Release 전부 생성 완료.
  v1.6.3 폰 설치 및 AI 러닝 요약 실기기 정상 동작 확인 완료 (2026-07-23)
- **실행 방법**: `flutter run` 또는 `flutter build apk --release` (JDK 21, `unset JAVA_HOME` 필요 — 시스템 JAVA_HOME이 깨진 JDK11)

## 최근 작업

- **2026-07-24**: 분석 탭 러닝 캘린더를 `일월화수목금토` 순서로 변경. 첫날 앞 빈칸을
  `DateTime.weekday % 7`로 계산하고 일요일·토요일 주말 색상 열을 각각 0·6으로 조정
- **2026-07-24**: 수동 추가·편집 화면에 `소요시간으로 입력` / `페이스로 입력` 모드를 추가.
  거리+페이스로 소요시간을 계산하고 모드 전환 때 파생값을 동기화하며, 저장 스키마는 기존
  `distanceM`·`durationSec` 그대로 유지. 5km·6분00초/km → 30:00 계산 확인

## 알려진 이슈

- 스플릿이 평균 페이스로만 나옴 — 삼성헬스가 HC에 상세 데이터를 안 내보내는 정책 한계 (해결 불가)
- 홈 화면 버전 라벨(`home_screen.dart`)이 하드코딩 문자열 — pubspec.yaml 버전 올릴 때마다 수동으로 같이 바꿔야 함
- 자동 업데이트 다운로드·설치 플로우 자체(앱 내 "업데이트 확인"으로 새 버전 받기)는 아직 미검증
  (지금까지는 매번 수동 설치로 테스트함 — 다음 배포 때 실제로 검증 필요)
- 캘린더 빈 날짜 추가·일요일 시작 변경·수동 기록 편집·페이스 입력 기능은 코드 검증만
  마쳤고 실기기 확인 전

## 다음 TODO

1. [ ] 폰에서 캘린더 일요일 시작 배치와 수동 추가·편집의 두 입력 모드 저장 확인
2. [ ] 폰에서 정기 동기화(↻)가 최근 30일을 매번 재스캔하는지, 중복 저장 없는지 확인
3. [ ] 다음 버전 배포 때 "업데이트 확인" 메뉴로 실제 자동 업데이트 다운로드·설치 플로우 검증
4. [ ] 진단 화면(`debug_screen.dart`, `getRawSessions`/`getPlannedSessions`)을 프로덕션에 남길지 제거할지 결정

**Phase 2 후보 (보류):**
6. [ ] 23일 러닝 프로그램 트래커
7. [ ] 목표 설정 기능
8. [ ] 공유 카드
9. [ ] 데이터 내보내기 (CSV/GPX 백업)
10. [ ] 캘린더 히트맵 고도화, 업적 Lottie 애니메이션
